name: All jobs
on: push
jobs:
   validatefiles:
     runs-on: Ubantu-latest
     steps:
      - name: validate html
        uses: actions/checkout@v4

      - name: validate JS
        uses: actions/checkout@v4
    